<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:fontFamily">@font/source_sans_pro_regular</item>
    </style>

    <style name="KeyboardNumbers" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColor">@color/keyboard_text_color</item>
        <item name="android:layout_height">match_parent</item>

        <item name="android:textSize">@dimen/keyboard_text_size</item>
        <item name="android:textAlignment">center</item>
        <!--        <item name="android:padding">@dimen/keyboard_view_padding</item>-->
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="Widget.App.Button.TextButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColor">@color/keyboard_text_color</item>
        <item name="android:layout_height">match_parent</item>

        <item name="android:textSize">@dimen/keyboard_text_size</item>
        <item name="android:textAlignment">center</item>
        <!--        <item name="android:padding">@dimen/keyboard_view_padding</item>-->
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_weight">1</item>
    </style>
</resources>
